form Box1HouseOwning {
   bool ifs = "Did you sell a house in 2010?" 
   bool hasBoughtHouse="Did you by a house in 2010?" 
   bool 
   hasMaintLoan=  
   "Did you enter a loan for maintenance/reconstruction?"
//kfjgkh
/*what?
sd
sd
*/
 	//fgkjdf //dfgk /*gggggggg*/
 	sd//s//
//sdfsdlfksdjghlkj

	if (6) {	
		bool name /*dfg*/= "label" 5
		bool name = "label" 5
		bool name = "label" 5
	 } else if (5)  {
	 	bool name = "label" 5
	 } else { int g = " df " }

  bool sellingPrice ="Price the house was sold for:"


   if (hasSoldHouse) {
     int sellingPrice ="Price the house was sold for:"
     int privateDebt= "Private debts for the sold house:" 
     int valueResidue =  "Value residue:"(sellingPrice - privateDebt)
   } else {
   	 int sellingPrice ="Price the house was sold for:"
    
   }
 	
	if (hasSoldHouse) {
		int sellingPrice ="Price the house was sold for:"
		if (bal) {
			bool dfj = "meh"
		}
	} else {
		int sellingPrice ="Price the house was sold for:"
   }
   
     int privateDebt= "Private debts for the sold house:" 
     int valueResidue =  "Value residue:"(sellingPrice - privateDebt)
 
		// just one if here

     
     // ifblock
   if (hasSoldHouse) {
     int sellingPrice ="Price the house was sold for:"
     int privateDebt= "Private debts for the sold house:" 
     int valueResidue =  "Value residue:"(sellingPrice - privateDebt)
   }
   
   // checking all the if else possibilities
     
   
   if (hasSoldHouse) // this is a single if
     {int sellingPrice = "Price the house was sold for:"sellingPrice
    }else//with a single else
     {string what = "what?"s56
   
   }
   if (blah) {
     int privateDebt= "Private debts for the sold house:"787878
     int sellingPrice ="Price the house was sold for:"  sf
   } else {
     int privateDebt= "Private debts for the sold house:" 
     int sellingPrice ="Price the house was sold for:"  
   }  
}