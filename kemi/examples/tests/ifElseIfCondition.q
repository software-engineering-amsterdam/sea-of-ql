form ifElseIfCondition {
  "Question one?"
    integer numberOne
  if(numberOne == 10) {
  	"Question two?"
  	  date theDate
  } else if(a == b) {
  	// We also have else if statements.
  	"Foo Bar?"
  	  string aText
  } else if(c == d) {
  	// They can be arbitrarily nested
  	"Blah?"
  	  string bText
  }
}
