package org.uva.sea.ql.visitors;

import org.uva.sea.ql.ast.form.QLForm;

public class TypeChecker {
	public boolean check(QLForm form) {
		env.getCurrentFormName = form.getName();
		
		return form.getBody().accept(this);
	}
	
	
}
